# AWS Cognito OAuth2 Settings
# AWS Cognitoでユーザープールとアプリクライアントを作成し、
# 以下の値を設定してください

# AWS Region (例: ap-northeast-1)
AWS_REGION=ap-northeast-1

# Cognito User Pool ID (例: ap-northeast-1_XXXXXXXXX)
COGNITO_USER_POOL_ID=ap-northeast-1_XXXXXXXXX

# Cognito App Client ID
COGNITO_CLIENT_ID=your-cognito-app-client-id

# Cognito App Client Secret
COGNITO_CLIENT_SECRET=your-cognito-app-client-secret

# OAuth2 Proxy Cookie Secret (32文字のランダム文字列)
# 以下のコマンドで生成できます: openssl rand -base64 32 | head -c 32
OAUTH2_PROXY_COOKIE_SECRET=your-32-character-random-string 